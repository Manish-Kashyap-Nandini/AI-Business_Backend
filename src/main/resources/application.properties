# App
spring.application.name=AI_Business_Backend

# Server (Render will inject PORT)
server.port=${PORT:8080}
server.address=0.0.0.0

# --- Database (use env vars with defaults for local) ---
spring.datasource.url=${DB_URL:jdbc:mysql://trolley.proxy.rlwy.net:50879/railway}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:lElrpAnJrhqNhRVHegcOFybJVigYqocy}
spring.datasource.driver-class-name=${DB_DRIVER:com.mysql.cj.jdbc.Driver}


# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.MySQL8Dialect}

# Security (for local only; in prod, disable or use JWT/OAuth2)
spring.security.user.name=${SECURITY_USER:admin}
spring.security.user.password=${SECURITY_PASS:admin123}
